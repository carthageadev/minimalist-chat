# Frontend env — baked into the bundle at build time (Vite only exposes VITE_* to the client).
# Only the API key is needed; base URLs are public and hardcoded in src/lib/nvidia.ts.
VITE_API_KEY="nvapi-..."
